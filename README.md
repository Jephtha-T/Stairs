# Ambiguous Parallax

Simple Three.js project built around a `Stairs.glb` model.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

If you want to preview the production build:

```bash
npm run preview
```
